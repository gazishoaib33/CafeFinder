// ui.js
// ------------------------------------------------------------------
// Owns all DOM rendering: cafe cards, map marker info windows,
// skeleton loaders, empty states, and filter/sort controls.
// Reads app state and paints it — never fetches data itself.
//
// Will be filled in as we build:
//   - Cafe cards                (Step 6)
//   - Filters/sort UI            (Step 7)
//   - Favorites page rendering    (Step 8)
//   - Skeletons + empty states     (Step 11)
// ------------------------------------------------------------------

export const UI = {
  // Populated in later steps.
};
