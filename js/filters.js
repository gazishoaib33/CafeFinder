// filters.js
// ------------------------------------------------------------------
// Pure functions for filtering and sorting a list of cafe results.
// No DOM access here — takes an array in, returns an array out —
// which makes this easy to reason about and easy to test.
//
// Will be filled in during Step 7 (Filters and Sorting).
// ------------------------------------------------------------------

export const Filters = {
  // Populated in Step 7.
};
